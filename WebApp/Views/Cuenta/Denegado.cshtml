@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container text-center mt-5">
    <div class="card shadow-lg p-4 border-danger">
        <div class="card-body">
            <h1 class="display-4 text-danger"><i class="bi bi-shield-lock-fill"></i> Acceso Denegado</h1>
            <p class="lead">No tienes permisos para acceder a esta sección del sistema.</p>
            <a href="~/Home/Index" class="btn btn-primary mt-3">Volver al inicio</a>
        </div>
    </div>
</div>
