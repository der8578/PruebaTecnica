@using Data.Models.Formula
@model IEnumerable<FormulaModel>

<!-- Resultados de búsqueda -->
<div class="row mt-5">
    <div class="col-md-12">
        @if (Model != null && Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th></th>
                            <th>ID</th>
                            <th>Nombre</th>
                            <th>Cantiad</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <a href="@Url.Action("Edit", "Formula", new { Id = item.IdFormula })"
                                        target="_blank">Editar</a>
                                </td>
                                <td>@item.IdFormula</td>
                                <td>@item.Nombre</td>
                                <td>@item.Cantidad</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="alert alert-warning" role="alert">
                No se encontraron registros...
            </div>
        }
    </div>
</div>